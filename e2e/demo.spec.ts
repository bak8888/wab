import { test, expect } from '@playwright/test';
test('demo mode flow', async ({ page }) => {await page.goto('/');await expect(page.getByText('Demo Mode')).toBeVisible();await page.getByLabel('경기 입력').fill('아스널 vs 첼시');await page.getByRole('button',{name:'분석 시작'}).click();await expect(page.getByText('후보 경기 선택')).toBeVisible();await page.getByText('Premier League').click();await expect(page.getByText('최종 예상')).toBeVisible();await expect(page.getByText('전체 출처 목록')).toBeVisible();await page.getByRole('button',{name:'다시 분석'}).click();await expect(page.getByText('최종 예상')).toBeVisible();});
