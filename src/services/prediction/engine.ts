import { predictionConfig as c } from '@/config/prediction';import type { PlayerAvailability, PredictionFactor, SourceRecord } from '@/domain/schemas';
export function normalizeProbabilities(vals:number[]){const safe=vals.map(v=>Number.isFinite(v)?Math.max(0,v):0);const sum=safe.reduce((a,b)=>a+b,0)||1;let rounded=safe.map(v=>Math.round(v/sum*1000)/10);const diff=Math.round((100-rounded.reduce((a,b)=>a+b,0))*10)/10;rounded[0]=Math.round((rounded[0]+diff)*10)/10;return rounded}
function poisson(lambda:number,k:number){let f=1;for(let i=2;i<=k;i++)f*=i;return Math.exp(-lambda)*Math.pow(lambda,k)/f}
export function predictSoccer(input:{homeRating:number;awayRating:number;homePlayers:PlayerAvailability[];awayPlayers:PlayerAvailability[];restDayDiff:number;sources:SourceRecord[]}){const homeInj=Math.min(c.injuryGoalPenaltyCap,input.homePlayers.filter(p=>['doubtful','unavailable','suspended'].includes(p.status)).length*0.08);const awayInj=Math.min(c.injuryGoalPenaltyCap,input.awayPlayers.filter(p=>['doubtful','unavailable','suspended'].includes(p.status)).length*0.08);const rating=(input.homeRating-input.awayRating)*c.ratingGoalScale;let eh=1.35+c.homeAdvantageGoals+rating-awayInj+Math.max(-c.maxAdjustmentGoals,Math.min(c.maxAdjustmentGoals,input.restDayDiff*c.restDayGoalImpact));let ea=1.15-rating-homeInj;eh=Math.max(0.2,eh);ea=Math.max(0.2,ea);let h=0,d=0,a=0;for(let i=0;i<=c.poissonMaxGoals;i++)for(let j=0;j<=c.poissonMaxGoals;j++){const p=poisson(eh,i)*poisson(ea,j); if(i>j)h+=p; else if(i===j)d+=p; else a+=p}const [homeWinProbability,drawProbability,awayWinProbability]=normalizeProbabilities([h,d,a]);const factors:PredictionFactor[]=[{name:'홈 어드밴티지',impact:c.homeAdvantageGoals,direction:'home',sourceIds:['src-official-fixture'],description:'홈 경기 이점이 기대득점에 반영되었습니다.'},{name:'결장/불투명 선수',impact:awayInj-homeInj,direction:awayInj>homeInj?'home':'away',sourceIds:input.sources.map(s=>s.sourceId),description:'공개 출처가 연결된 출전 가능성만 반영했습니다.'}];return{algorithm:'team-rating + expected-goals poisson',modelVersion:c.modelVersion,homeWinProbability,drawProbability,awayWinProbability,expectedHomeGoals:Math.round(eh*100)/100,expectedAwayGoals:Math.round(ea*100)/100,predictedScoreRange:`${Math.round(eh)}-${Math.round(ea)} 주변`,confidence:'medium' as const,keyFactors:factors,limitations:['Demo Mode 통계는 fixture 기반 예시 데이터입니다.','공식 선발 명단 발표 전에는 선수 상태가 변경될 수 있습니다.']}}
